[package]
name = "axfs-ng-vfs"
version = "0.5.8"
edition.workspace = true
authors = ["Mivik <mivikq@gmail.com>", "朝倉水希 <asakuramizu111@gmail.com>"]
description = "Virtual filesystem layer for ArceOS"
keywords = ["vfs", "filesystem", "ax-fs-ng", "arceos"]
repository = "https://github.com/Starry-OS/axfs-ng-vfs"
license = "Apache-2.0"

[features]
axtest = ["dep:axtest"]
default = []
host-test = ["ax-sync/host-test"]
lockdep = ["ax-sync/lockdep"]

[dependencies]
ax-errno = { workspace = true }
ax-sync = { workspace = true, features = ["sleep"] }
axtest = { workspace = true, optional = true }
bitflags = "2.10"
cfg-if = "1"
hashbrown = "0.16"
inherit-methods-macro = "0.1"
log = "0.4"
smallvec = "1.15"

[dev-dependencies]
ax-sync.workspace = true

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(axtest)'] }
