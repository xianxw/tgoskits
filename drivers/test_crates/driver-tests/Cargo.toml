[package]
name = "driver-tests"
version = "0.1.0"
edition.workspace = true
publish = false
license.workspace = true
repository.workspace = true
autotests = false

[dependencies]

[dev-dependencies]
ax-driver = { workspace = true, features = ["serial"] }
ax-lazyinit.workspace = true
ax-sync = { workspace = true, features = ["host-test"] }
axklib.workspace = true
eth-intel.workspace = true
fdt-edit.workspace = true
pcie.workspace = true
rdif-intc.workspace = true
rdif-serial.workspace = true
rdrive.workspace = true
rockchip-soc.workspace = true
some-serial.workspace = true
num-align = "0.1.0"

[[test]]
name = "serial_fdt"
path = "tests/serial_fdt.rs"

# These bare-test cases stay disabled by default. To run one manually, add the
# required bare-test dependency in this test crate and enable the matching
# [[test]] target locally.
